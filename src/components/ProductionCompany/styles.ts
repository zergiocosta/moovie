import styled from 'styled-components/native'

const StyledCompanyWrapper = styled.View`
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 25px;
`

const StyledLabel = styled.Text`
  margin-horizontal: 10px;
  font-size: 14px;
  text-align: center;
`

export {
  StyledCompanyWrapper,
  StyledLabel
}
